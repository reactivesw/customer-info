apiVersion: v1
kind: Service
metadata:
  name: customer-info
  labels:
    name: customer-info
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30005
  selector:
    app: customer-info
